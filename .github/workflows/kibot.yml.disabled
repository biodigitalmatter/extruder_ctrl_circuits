name: generate files from kibot

on:
  push:
    # paths:
    #   - "**.sch"
    #   - "**.kicad_pcb"
  # pull_request:
  # paths:
  #   - "**.sch"
  #   - "**.kicad_pcb"

jobs:
  example:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
